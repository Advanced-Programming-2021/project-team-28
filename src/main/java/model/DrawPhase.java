package model;

import Enums.Turn;

public class DrawPhase extends Phase{
    public DrawPhase(Player firstPlayer, Player secondPlayer, Turn turn) {
        super(firstPlayer, secondPlayer, turn);
    }
}
