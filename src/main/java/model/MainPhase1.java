package model;

import enums.Turn;

public class MainPhase1 extends MainPhase{
    public MainPhase1(Player firstPlayer, Player secondPlayer, Turn turn) {
        super(firstPlayer, secondPlayer, turn);
    }
}
